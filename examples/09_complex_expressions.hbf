int BASE = 60;

void main() {
    int x = 2;
    int y = 5;
    
    // Multiple virtual ops resolved to literal print
    // HBF currently supports + and -
    cell result = BASE + x + y; 
    putc(result); // 60 + 2 + 5 = 67 ('C')
    
    putc('\n');
}
