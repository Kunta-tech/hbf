void main() {
    cell count = 5;
    
    // Generates a native BFO while loop (countdown)
    forn(count) {
        putc('*');
        // Loop counter is automatically managed in 'forn'
    }
    putc('\n');
}
main();