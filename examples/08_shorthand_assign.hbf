void main() {
    cell A = 65; // 'A'
    int i = 5;
    
    // Optimized shorthand: add(A, i)
    add(A, i);
    putc(A); // 'F' (70)
    
    // Reverse order: add(A, i)
    add(A, i);
    putc(A); // 'K' (75)
    
    putc('\n');
}
main();
